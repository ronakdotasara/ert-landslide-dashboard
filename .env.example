# Backend server
PORT=3001

# SQLite database path (relative to backend/)
DB_PATH=./ert.db

# Device auth key — must match DEVICE_KEY in firmware/main/config.h
DEVICE_KEY=your-secret-device-key

# Optional: set to 'production' for prod builds
NODE_ENV=development
